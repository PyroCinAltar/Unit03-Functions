# Scope - the visibility of variables, where it can be seen
# Global - outside all functions
# Local - inside a function(only visible there)

# The Bug - Crashes
# def add_bonus():
#     score = score + 100
    
# score = 500
# add_bonus()

# solutions: pass and return, using the "global" keyword